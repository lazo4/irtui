name: Rust

on:
  push:
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: ['macos-latest', 'ubuntu-latest', 'windows-latest']

    steps:
    - uses: actions/checkout@v4
    - if: startsWith(matrix.os, 'ubuntu')
      run: |
        sudo apt-get update
        sudo apt-get install -y libchafa-dev pkg-config libglib2.0-dev
    - if: matrix.os == 'macos-latest'
      run: brew install chafa
    - if: matrix.os == 'windows-latest'
      run: winget install pkgconfiglite.pkgconfiglite hpjansson.Chafa --accept-package-agreements --accept-source-agreements
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --verbose
